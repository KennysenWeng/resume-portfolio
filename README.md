# Resume Portfolio Editor

本專案已分成前端與後端：

- 前端：`public/`
- 後端：`server.js`
- 儲存檔案：`data/resume.json`

## 使用方式

```powershell
npm start
```

啟動後開啟：

```text
http://localhost:5173
```

在網頁上編輯履歷後，按右上角「儲存檔案」，後端會將目前履歷內容寫入 `data/resume.json`。

「列印」可使用瀏覽器列印或另存 PDF。

完整使用說明請看：

```text
操作手冊.md
```
